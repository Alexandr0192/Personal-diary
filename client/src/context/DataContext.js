// Файл DataContext.js
import {createContext} from "react";

/*
    Передача данных поста
 */
export const PostDataContext = createContext(null);

/*
    Передача названия поста
 */
export const TitlePostDataContext = createContext(null);

