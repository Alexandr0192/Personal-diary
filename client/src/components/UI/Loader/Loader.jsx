import React from 'react';
import cl from './Loader.module.css';

/**
 * Иконка загрузки
 * @returns {JSX.Element}
 *
 */
const Loader = () => {
    return (
        <div className={cl.loader}>
            
        </div>
    );
};

export default Loader;